[pytest]
python_files = test_*.py
python_functions = test_*
testpaths = .
norecursedirs = .* __pycache__ captures docs alpha
addopts = -ra
markers =
    slow: minutes-long checks (rough calibration, filters, Monte Carlo); CI runs them in the full tier
filterwarnings =
    ignore::RuntimeWarning
    ignore::UserWarning
